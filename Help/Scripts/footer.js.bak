/* footer.js: displays HR and small type
at the bottom of the page.
*/
 
function showfooter(OnDiv)
{
	
	OnDiv.innerHTML='<HR><SMALL>Copyright 2005-2007 <A HREF="http://www.geocities.com/BC_Programming/index.html">BASeCamp corporation</A></SMALL>';
	
}

function updatecode()
{
	//var codes=document.getElementsByTagName("code");
	//var currcode;
	//window.alert(codes.length);
	var cells = document.getElementsByTagName("code"); 
	for (var i = 0; i < cells.length; i++) { 
    //status = cells[i].getAttribute("status"); 
    //if ( status == "open") { 
    //    // grab the data 
    //}
    //window.alert(i);
    //window.alert(cells[i]);
    window.alert(getOuterHTML(cells[i]));
}

	
	
}